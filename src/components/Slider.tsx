export const Slider = () => {
  return (
    <div
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(4, 1fr)",
        transition: "all 1s ease-in-out",
      }}
    ></div>
  );
};
